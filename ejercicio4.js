// 1. Mostrar un mensaje en la consola del navegador
console.log("¡Hola! El archivo JavaScript se ha cargado correctamente.");

// 2. Crear variables (guardar datos)
const nombre = "Carlos";
let edad = 25;

// 3. Crear una función (un bloque de código reutilizable)
function saludar(usuario) {
    console.log("Hola, " + usuario + ". ¡Bienvenido a la programación!");
}

// 4. Ejecutar (llamar) la función
saludar(nombre);